<section class="interior-condition section">
  <div class="container">
    <div class="interior-condition__heading" data-reveal="up">
      <div class="interior-condition__eyebrow">Condition Matters</div>

      <h2>
        Some Interiors Need <span>More Work</span>
      </h2>

      <p>
        Standard cleaning covers everyday buildup.
        Pet hair and deeper stains can require additional time and treatment.
      </p>
    </div>

    <div class="interior-condition__layout">
      <!-- LARGE CARD -->
      <article class="condition-card condition-card--large">
        <img
          src="/media/interior/pet-hair-interior.jpg"
          alt="Pet hair in vehicle interior"
          width="1280"
          height="960"
          loading="lazy"
          decoding="async"
        />

        <div class="condition-card__overlay"></div>

        <div class="condition-card__copy" data-reveal="left"
style="--reveal-delay: 80ms;">
          <div class="interior-condition__eyebrow">
            Pet Hair
          </div>

          <h3>
            The amount matters more than simply having a pet
          </h3>

          <p>
            Light pet hair is included.
            Moderate removal is +$30,
            heavy is +$50,
            and extreme cases require a custom quote.
          </p>
        </div>
      </article>

      <!-- RIGHT COLUMN -->
      <div class="interior-condition__side" data-reveal="right"
style="--reveal-delay: 120ms;">
        <article class="condition-card condition-card--small">
          <img
            src="/media/interior/stained-upholstery.webp"
            alt="Stain in vehicle upholstery"
            width="1448"
            height="1086"
            loading="lazy"
            decoding="async"
          />

          <div class="condition-card__overlay"></div>

          <div class="condition-card__copy">
            <div class="interior-condition__eyebrow">
              Stains
            </div>

            <h3>Surface or soaked in?</h3>

            <p>
              Light spots are addressed when possible.
              More extensive stain treatment starts from +$50.
            </p>
          </div>
        </article>

        <article class="condition-card condition-card--small">
          <img
            src="/media/interior/clean-interior-condition.jpg"
            alt="Clean vehicle interior"
            width="960"
            height="1280"
            loading="lazy"
            decoding="async"
          />

          <div class="condition-card__overlay"></div>

          <div class="condition-card__copy">
            <div class="interior-condition__eyebrow">
              Everyday Use
            </div>

            <h3>Sometimes it just needs a reset</h3>

            <p>
              Dust, crumbs, fingerprints and routine buildup
              are exactly what Interior Detailing is designed for.
            </p>
          </div>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
  .interior-condition {
    background: var(--color-bg);
  }

  .interior-condition__heading {
    max-width: 780px;
    margin: 0 auto 46px;

    text-align: center;
  }

  .interior-condition__eyebrow {
    margin-bottom: 12px;

    color: var(--color-cyan);

    font-size: 12px;
    font-weight: 900;
    letter-spacing: 3px;
    line-height: 1.3;

    text-transform: uppercase;
  }

  .interior-condition__heading h2 {
    margin: 0 0 18px;

    color: #fff;

    font-size: clamp(34px, 4.2vw, 54px);
    line-height: 1.08;
    font-weight: 900;
  }

  .interior-condition__heading h2 span {
    background: linear-gradient(
      90deg,
      var(--color-cyan),
      #3b8cff,
      #8d49ec
    );

    -webkit-background-clip: text;
    background-clip: text;

    color: transparent;
  }

  .interior-condition__heading > p {
    max-width: 760px;

    margin: 0 auto;

    color: var(--color-text-muted);

    font-size: 18px;
    line-height: 1.78;
  }

  .interior-condition__layout {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;

    gap: 18px;
  }

  .interior-condition__side {
    display: grid;
    grid-template-rows: 1fr 1fr;

    gap: 18px;
  }

  .condition-card {
    position: relative;

    overflow: hidden;

    border: 1px solid var(--color-border);
    border-radius: 24px;

    background: #080914;

    isolation: isolate;

    transition:
      transform var(--transition-base),
      border-color var(--transition-base);
  }

  .condition-card:hover {
    transform: translateY(-5px);

    border-color: rgba(8, 215, 223, 0.3);
  }

  .condition-card--large {
    min-height: 570px;
  }

  .condition-card--small {
    min-height: 276px;
  }

  .condition-card img {
    position: absolute;
    inset: 0;

    z-index: -2;

    width: 100%;
    height: 100%;

    object-fit: cover;

    transition: transform 0.6s ease;
  }

  .condition-card:hover img {
    transform: scale(1.035);
  }

  .condition-card__overlay {
    position: absolute;
    inset: 0;

    z-index: -1;

    background:
      linear-gradient(
        0deg,
        rgba(5, 6, 17, 0.98) 0%,
        rgba(5, 6, 17, 0.72) 38%,
        rgba(5, 6, 17, 0.12) 76%
      );
  }

  .condition-card__copy {
    position: absolute;

    left: 0;
    right: 0;
    bottom: 0;

    padding: 30px;
  }

  .condition-card__copy h3 {
    max-width: 580px;

    margin: 0 0 9px;

    color: #fff;

    font-size: 26px;
    line-height: 1.2;
    font-weight: 900;
  }

  .condition-card--small .condition-card__copy h3 {
    font-size: 21px;
  }

  .condition-card__copy p {
    max-width: 580px;

    margin: 0;

    color: #c4c7d7;

    font-size: 14px;
    line-height: 1.65;
  }

  @media (max-width: 900px) {
    .interior-condition__layout {
      grid-template-columns: 1fr;
    }

    .condition-card--large {
      min-height: 500px;
    }

    .interior-condition__side {
      grid-template-columns: 1fr 1fr;
      grid-template-rows: auto;
    }

    .condition-card--small {
      min-height: 340px;
    }
  }

  @media (max-width: 600px) {
    .interior-condition__side {
      grid-template-columns: 1fr;
    }

    .condition-card--large,
    .condition-card--small {
      min-height: 410px;
    }

    .condition-card__copy {
      padding: 23px;
    }

    .condition-card__copy h3,
    .condition-card--small .condition-card__copy h3 {
      font-size: 22px;
    }

    .interior-condition__heading > p {
      font-size: 16px;
    }
  }
</style>
