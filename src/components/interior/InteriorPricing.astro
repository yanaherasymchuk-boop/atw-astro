<section class="interior-pricing section">
  <div class="container">
    <div class="interior-pricing__heading" data-reveal="up">
      <div class="interior-pricing__eyebrow">
        Straightforward Pricing
      </div>

      <h2>
        Interior Detailing <span>Pricing</span>
      </h2>

      <p>
        Pricing is based mainly on vehicle size.
        Heavy contamination or additional work is priced separately.
      </p>
    </div>

    <div class="interior-pricing__wrap" data-reveal="up"
style="--reveal-delay: 100ms;">
      <div class="interior-pricing__main">
        <div>
          <h3>Interior Detail</h3>

          <p>
            Complete interior cleaning without exterior work.
          </p>
        </div>

        <div class="interior-pricing__prices">
          <div class="interior-pricing__box">
            <small>Sedan</small>
            <strong>$200+</strong>
          </div>

          <div class="interior-pricing__box">
            <small>SUV</small>
            <strong>$225+</strong>
          </div>

          <div class="interior-pricing__box">
            <small>XL SUV / Pickup</small>
            <strong>$250+</strong>
          </div>

          <div class="interior-pricing__box">
            <small>Van</small>
            <strong>$300+</strong>
          </div>
        </div>
      </div>

      <div class="interior-pricing__extra" data-reveal="up"
style="--reveal-delay: 160ms;">
        <div class="interior-pricing__extra-card">
          <strong>
            Seat Extraction with Interior / Full Detail
          </strong>

          <span>
            Sedan / SUV: +$100<br />
            XL SUV / Pickup / Van: +$175
          </span>
        </div>

        <div class="interior-pricing__extra-card">
          <strong>
            Standalone or partial work
          </strong>

          <span>
            Full standalone Seat Extraction starts from $250.
            One seat, several seats, one row, cargo area only,
            or another partial service is quoted individually.
          </span>
        </div>
      </div>

      <div class="interior-pricing__buttons">
        <a
          class="interior-pricing__btn interior-pricing__btn--cyan"
          href="/mobile-detailing-price-estimate/"
        >
          Check Your Vehicle →
        </a>

        <a
          class="interior-pricing__btn interior-pricing__btn--violet"
          href="sms:+12533867243"
        >
          Text Photos for a Quote →
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .interior-pricing {
    background: var(--color-bg);
  }

  .interior-pricing__heading {
    max-width: 780px;
    margin: 0 auto;
    text-align: center;
  }

  .interior-pricing__eyebrow {
    margin-bottom: 14px;

    color: var(--color-cyan);

    font-size: 12px;
    font-weight: 900;
    letter-spacing: 3px;
    line-height: 1.3;

    text-transform: uppercase;
  }

  .interior-pricing__heading h2 {
    margin: 0 0 18px;

    color: #fff;

    font-size: clamp(34px, 4.2vw, 54px);
    line-height: 1.08;
    font-weight: 900;
  }

  .interior-pricing__heading h2 span {
    background: linear-gradient(
      90deg,
      var(--color-cyan),
      #3b8cff,
      #8d49ec
    );

    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .interior-pricing__heading p {
    max-width: 760px;
    margin: 0 auto;

    color: var(--color-text-muted);

    font-size: 18px;
    line-height: 1.78;
  }

  .interior-pricing__wrap {
    width: min(1080px, 100%);

    margin: 44px auto 0;
    padding: 34px;

    border: 1px solid rgba(8, 215, 223, 0.29);
    border-radius: 26px;

    background:
      radial-gradient(
        circle at 74% 0%,
        rgba(118, 33, 200, 0.11),
        transparent 42%
      ),
      rgba(255, 255, 255, 0.022);
  }

  .interior-pricing__main {
    display: grid;
    grid-template-columns: 250px 1fr;

    gap: 28px;

    align-items: center;
  }

  .interior-pricing__main h3 {
    margin: 0 0 7px;

    color: #fff;

    font-size: 29px;
    font-weight: 900;
  }

  .interior-pricing__main p {
    margin: 0;

    color: var(--color-text-muted);

    font-size: 13px;
    line-height: 1.58;
  }

  .interior-pricing__prices {
    display: grid;
    grid-template-columns: repeat(4, 1fr);

    gap: 13px;
  }

  .interior-pricing__box {
    padding: 20px 12px;

    border: 1px solid var(--color-border);
    border-radius: 17px;

    background: rgba(3, 4, 13, 0.39);

    text-align: center;

    transition:
      transform var(--transition-fast),
      border-color var(--transition-fast),
      background var(--transition-fast);
  }

  .interior-pricing__box:hover {
    transform: translateY(-5px);

    border-color: rgba(8, 215, 223, 0.35);
    background: rgba(8, 215, 223, 0.045);
  }

  .interior-pricing__box small {
    display: block;

    margin-bottom: 6px;

    color: #b8bbce;

    font-size: 13px;
    font-weight: 800;
  }

  .interior-pricing__box strong {
    color: #fff;

    font-size: 29px;
    font-weight: 900;
  }

  .interior-pricing__extra {
    display: grid;
    grid-template-columns: 1fr 1fr;

    gap: 18px;

    margin-top: 25px;
    padding-top: 23px;

    border-top: 1px solid var(--color-border);
  }

  .interior-pricing__extra-card {
    padding: 20px;

    border: 1px solid var(--color-border);
    border-radius: 18px;

    background: rgba(255, 255, 255, 0.018);
  }

  .interior-pricing__extra-card strong {
    display: block;

    margin-bottom: 6px;

    color: #fff;
  }

  .interior-pricing__extra-card span {
    color: var(--color-text-muted);

    font-size: 13px;
    line-height: 1.62;
  }

  .interior-pricing__buttons {
    display: flex;
    align-items: center;
    flex-wrap: wrap;

    gap: 14px;

    margin-top: 26px;
  }

  .interior-pricing__btn {
    min-height: 50px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 0 28px;

    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: var(--radius-pill);

    color: #fff;

    font-size: 13px;
    font-weight: 900;

    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.18);

    transition:
      transform var(--transition-fast),
      filter var(--transition-fast);
  }

  .interior-pricing__btn:hover {
    transform: translateY(-3px);
    filter: brightness(1.1);
  }

  .interior-pricing__btn--cyan {
    color: #061218;

    background: linear-gradient(
      100deg,
      #0ce1df,
      #18c8d7,
      #20aeda
    );
  }

  .interior-pricing__btn--violet {
    background: linear-gradient(
      105deg,
      #922bdd,
      #5e19dd,
      #2413c3
    );
  }

  @media (max-width: 900px) {
    .interior-pricing__main {
      grid-template-columns: 1fr;
    }

    .interior-pricing__prices {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 600px) {
    .interior-pricing__wrap {
      padding: 23px 17px;
    }

    .interior-pricing__prices,
    .interior-pricing__extra {
      grid-template-columns: 1fr;
    }

    .interior-pricing__buttons {
      flex-direction: column;
      align-items: stretch;
    }

    .interior-pricing__btn {
      width: 100%;
    }

    .interior-pricing__heading p {
      font-size: 16px;
    }
  }
</style>