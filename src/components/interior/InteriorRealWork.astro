<section class="interior-work section">
  <div class="container">
    <div class="interior-work__layout">
      <div class="interior-work__copy" data-reveal="up">
        <div class="interior-work__eyebrow">
          Real ATW Work
        </div>

        <h2>
          Real Cars. <span>Real Results.</span>
        </h2>

        <p>
          These are actual vehicles we worked on.
          Different areas and different jobs,
          shown without pretending unrelated photos are a perfect before-and-after pair.
        </p>

        <div class="interior-work__cta" data-reveal="up"
style="--reveal-delay: 100ms;">
          <a
            class="interior-work__button"
            href="https://gallery.atwdetailing.com/"
            target="_blank"
            rel="noopener"
          >
            See More Real Results →
          </a>
        </div>
      </div>

      <div class="interior-work__grid">
        <div class="interior-work__shot">
          <img
            src="https://atwdetailing.com/wp-content/uploads/2026/08/photo_25_2026-07-25_13-16-48.jpg"
            alt="Vehicle interior before detailing"
            loading="lazy"
          />

          <span class="interior-work__tag">
            BEFORE
          </span>
        </div>

        <div class="interior-work__shot">
          <img
            src="https://atwdetailing.com/wp-content/uploads/2026/08/photo_27_2026-07-25_13-16-49.jpg"
            alt="Vehicle interior before detailing"
            loading="lazy"
          />

          <span class="interior-work__tag">
            BEFORE
          </span>
        </div>

        <div class="interior-work__shot">
          <img
            src="https://atwdetailing.com/wp-content/uploads/2026/08/photo_36_2026-07-25_13-16-49.jpg"
            alt="Vehicle interior after detailing"
            loading="lazy"
          />

          <span class="interior-work__tag">
            AFTER
          </span>
        </div>

        <div class="interior-work__shot">
          <img
            src="https://atwdetailing.com/wp-content/uploads/2026/08/photo_37_2026-07-25_13-16-49.jpg"
            alt="Vehicle interior after detailing"
            loading="lazy"
          />

          <span class="interior-work__tag">
            AFTER
          </span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .interior-work {
    background: var(--color-bg-soft);
  }

  .interior-work__layout {
    display: grid;
    grid-template-columns: 0.84fr 1.16fr;

    gap: 55px;

    align-items: center;
  }

  .interior-work__eyebrow {
    margin-bottom: 14px;

    color: var(--color-cyan);

    font-size: 12px;
    font-weight: 900;
    letter-spacing: 3px;
    line-height: 1.3;

    text-transform: uppercase;
  }

  .interior-work__copy h2 {
    margin: 0 0 18px;

    color: #fff;

    font-size: clamp(34px, 4.2vw, 54px);
    line-height: 1.08;
    font-weight: 900;
  }

  .interior-work__copy h2 span {
    background: linear-gradient(
      90deg,
      var(--color-cyan),
      #3b8cff,
      #8d49ec
    );

    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .interior-work__copy p {
    max-width: 760px;

    margin: 0;

    color: var(--color-text-muted);

    font-size: 18px;
    line-height: 1.78;
  }

  .interior-work__cta {
    margin-top: 24px;
  }

  .interior-work__button {
    min-height: 50px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 0 28px;

    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: var(--radius-pill);

    background: linear-gradient(
      105deg,
      #922bdd,
      #5e19dd,
      #2413c3
    );

    color: #fff;

    font-size: 13px;
    font-weight: 900;

    box-shadow: 0 12px 34px rgba(102, 28, 219, 0.2);

    transition:
      transform var(--transition-fast),
      filter var(--transition-fast);
  }

  .interior-work__button:hover {
    transform: translateY(-3px);
    filter: brightness(1.1);
  }

  .interior-work__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;

    gap: 12px;
  }

  .interior-work__shot {
    position: relative;

    min-height: 255px;

    overflow: hidden;

    border: 1px solid var(--color-border);
    border-radius: 20px;

    transition:
      transform var(--transition-base),
      border-color var(--transition-base);
  }

  .interior-work__shot:hover {
    transform: translateY(-5px);

    border-color: rgba(8, 215, 223, 0.35);
  }

  .interior-work__shot img {
    width: 100%;
    height: 100%;

    min-height: 255px;

    object-fit: cover;

    transition: transform 0.5s ease;
  }

  .interior-work__shot:hover img {
    transform: scale(1.04);
  }

  .interior-work__tag {
    position: absolute;

    left: 13px;
    bottom: 13px;

    padding: 6px 10px;

    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: var(--radius-pill);

    background: rgba(5, 6, 17, 0.72);

    color: #fff;

    font-size: 10px;
    font-weight: 900;
    letter-spacing: 1.2px;
  }

  @media (max-width: 900px) {
    .interior-work__layout {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 600px) {
    .interior-work__grid {
      grid-template-columns: 1fr 1fr;
    }

    .interior-work__shot,
    .interior-work__shot img {
      min-height: 190px;
    }

    .interior-work__copy p {
      font-size: 16px;
    }
  }
</style>